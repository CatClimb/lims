<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.dao.DeviceDao">
    <resultMap id="DeviceResultMap" type="deviceEntity">
<id property="id" column="id"></id>
<result property="devName" column="dev_name"></result>
<result property="devPrice" column="dev_price"></result>
<result property="devUStatus" column="dev_u_status"></result>
<result property="devStatus" column="dev_status"></result>
<result property="name" column="name"></result>
<result property="deviceSTime" column="device_s_time"></result>
<result property="deviceETime" column="device_e_time"></result>
<result property="devReason" column="dev_reason"></result>
<result property="objName" column="obj_name"></result>
    </resultMap>

    <insert id="insert">
        INSERT device(dev_name,dev_price) VALUES(#{devName},#{devPrice})
    </insert>

    <update id="update">
        UPDATE device
    SET dev_name=#{devName},dev_price=#{devPrice},dev_u_status=#{devUStatus},dev_status=#{devStatus},name=#{name},device_s_time=#{deviceSTime},device_e_time=#{deviceETime},dev_reason=#{devReason},obj_name=#{objName}
    WHERE id=#{id}
    </update>

    <select id="conditionalQuery" resultMap="DeviceResultMap">
        SELECT *
        FROM device
        WHERE
            dev_name LIKE CONCAT('%',#{devName},'%')
           OR dev_price LIKE CONCAT('%',#{devPrice})
           OR dev_u_status LIKE CONCAT('%',#{devUStatus},'%')
           OR dev_status LIKE CONCAT('%',#{devStatus},'%')
           OR name LIKE CONCAT('%',#{name},'%')
           OR device_s_time LIKE CONCAT('%',#{deviceSTime},'%')
           OR device_e_time LIKE CONCAT('%',#{deviceETime},'%')
           OR dev_reason LIKE CONCAT('%',#{devReason},'%')
           OR obj_name LIKE CONCAT('%',#{objName},'%')
        ORDER BY id ASC
            LIMIT #{start},#{pageSize}
    </select>

    <delete id="deleteById">
        DELETE FROM device
        WHERE id=#{id}
    </delete>

    <select id="conditionalQueryCount" resultType="int">
            SELECT COUNT(*) FROM
            (SELECT *
             FROM device
             WHERE
                 dev_name LIKE CONCAT('%',#{devName},'%')
                OR dev_price LIKE CONCAT('%',#{devPrice})
                OR dev_u_status LIKE CONCAT('%',#{devUStatus},'%')
                OR dev_status LIKE CONCAT('%',#{devStatus},'%')
                OR name LIKE CONCAT('%',#{name},'%')
                OR device_s_time LIKE CONCAT('%',#{deviceSTime},'%')
                OR device_e_time LIKE CONCAT('%',#{deviceETime},'%')
                OR dev_reason LIKE CONCAT('%',#{devReason},'%')
                OR obj_name LIKE CONCAT('%',#{objName},'%')
             ORDER BY id ASC
                 LIMIT #{start},#{pageSize})
                tmp
    </select>
</mapper>
